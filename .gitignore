node_modules
.tmp
dist

# OS / editor junk
.DS_Store

# pbiviz build-analyzer reports (generated by pbiviz package)
webpack.statistics.*.html

# ── Local-only: dev/build tooling, tests & docs. Kept on disk to work with,
# ── but deliberately OFF the published GitHub repo, which holds ONLY the
# ── shippable visual (src/, style/, assets/ + build config). See visuals/CLAUDE.md.
test/
harness/
autotest/
scripts/
docs/
.claude/
CLAUDE.md
jest.config.js
# stray QA handoff notes (never on GitHub)
QA-RESULTS-windows.md
WINDOWS-QA-HANDOFF.md
